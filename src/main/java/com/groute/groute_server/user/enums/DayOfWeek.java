package com.groute.groute_server.user.enums;

/**
 * 요일.
 * 알림 설정(MYP003) 등에서 요일 체크박스 다중 선택에 사용.
 */
public enum DayOfWeek {
    /** 월요일. */
    MON,
    /** 화요일. */
    TUE,
    /** 수요일. */
    WED,
    /** 목요일. */
    THU,
    /** 금요일. */
    FRI,
    /** 토요일. */
    SAT,
    /** 일요일. */
    SUN
}
