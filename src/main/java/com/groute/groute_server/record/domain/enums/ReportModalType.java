package com.groute.groute_server.record.domain.enums;

/** 홈 복귀 시 노출되는 리포트 모달 종류. */
public enum ReportModalType {
    /** STAR 10개 달성 시 노출되는 미니 모달. */
    MINI,
    /** STAR 20개 이상, 10 배수마다 노출되는 풀 모달. */
    FULL
}
